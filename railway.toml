[build]
builder = "dockerfile"
dockerfilePath = "./FireStoreIoTAppointmentsService/Dockerfile"

[deploy]
startCommand = "/app/FireStoreIoTAppointmentsService"
restartPolicyType = "always"
healthcheckPath = "/health"